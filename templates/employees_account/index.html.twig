{% extends 'layout.html.twig' %}

{% block title %}Účty zaměstnance - {{ employee.name }}{% endblock %}

{% block content %}
    <section>
        <h2>Účty pro {{ employee.name }}</h2>
        <p>Role: {{ employee.role.title }}</p>

        <a href="{{ path('account_new', { 'employee_id': employee.id }) }}" class="button"> ➕ Add New Account</a>

        <table>
            <thead>
            <tr>
                <th>Název účtu</th>
                <th>Typ účtu</th>
                <th>Expirace</th>
                <th>Upravit</th>
                <th>Odstranit</th>
            </tr>
            </thead>
            <tbody>
            {% for account in accounts %}
                <tr>
                    <td>{{ account.name }}</td>
                    <td>{{ account.type }}</td>
                    <td>{{ account.expiration }}</td>
                    <td>
                        <a href="{{ path('account_edit', { 'id': account.id }) }}" class="action-button btn-edit">
                            <span class="icon">✏️</span> Edit
                        </a>
                    </td>
                    <td>
                        <form action="{{ path('account_delete', { 'id': account.id }) }}" method="post" style="display:inline;">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ account.id) }}">
                            <button type="submit" class="action-button btn-delete" onclick="return confirm('Are you sure you want to delete this account?');">
                                <span class="icon">🗑️</span> Delete
                            </button>
                        </form>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5">Žádné účty nejsou dostupné.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </section>
{% endblock %}
